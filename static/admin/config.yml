backend:
  name: git-gateway
  branch: main

media_folder: "src/assets/images"
public_folder: "static"

collections:
  - name: "text"
    label: "text"
    delete: false
    files:
      - name: text
        label: text
        file: "content/text.yml"
        fields:
          - name: languages
            widget: list
            fields:
            - { name: language, widget: string }
            - { name: siteName, widget: string }
            - { name: introduction, widget: text }
            - { name: aboutHeader, widget: string }
            - { name: experienceHeader, widget: string }
            - { name: contactHeader, widget: string }
            - { name: contactIntroduction, widget: text }
            - { name: copyright, widget: string }
  - name: "about"
    label: "about"
    description: "more info about myself/work/life"
    folder: "content/_about/"
    create: true
    fields:
      - { name: language, widget: string }
      - { name: title, widget: string }
      - { name: memo, widget: markdown }
      - { name: tags, widget: relation, collection: tag, valueField: tag.*, searchFields: [tag.title] }
  - name: "experience"
    label: "experience"
    description: "work and education experience in development"
    folder: "content/_experience/"
    create: true
    fields:
      - { name: language, widget: string }
      - { name: from, widget: datetime }
      - { name: to, widget: datetime }
      - { name: title, widget: string }
      - { name: memo, widget: markdown }
      - { name: link, widget: string, required: false }
      - { name: tags, widget: relation, collection: tag, valueField: tag.*, searchFields: [tag.title] }
  - name: "contact"
    label: "contact"
    desciption: "ways of contacting me"
    folder: "content/_contact/"
    create: true
    fields:
      - { name: language, widget: string }
      - { name: icon, widget: string }
      - { name: link, widget: string }
      - { name: title, widget: string }
      - { name: handle, widget: string, required: false }
  - name: "tag"
    label: "tags"
    description: "tags to emphasise and link other content"
    folder: "content/_tag/"
    create: true
    fields:
      - { name: icon, widget: string }
      - { name: title, widget: string }
